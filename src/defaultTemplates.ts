import { CharacterConfig, Macro } from './types';

// The default initial characters (completely clean and empty of any examples)
export const DEFAULT_CHARACTERS: CharacterConfig[] = [];

export const SAMPLE_DIALOGUES: Macro[] = [];

